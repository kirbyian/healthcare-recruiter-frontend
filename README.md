# Healthcare Recruiter – Frontend

A modern React/TypeScript single-page application (SPA) built for healthcare recruitment job posting workflows. 
This frontend interfaces with the backend services to present recruiters with job postings, and status dashboards.

## Features
- Responsive UI built with React (Create React App) and TS for type-safety.  
- Recruiter-focused workflows: job listing management
- API integration to the corresponding backend service for data retrieval and updates.  
- Build and deploy ready: `npm run build` creates optimized production assets.  

## Getting Started
1. Clone the repo and install dependencies:  
   ```bash
   npm install
